Here are all the commands that were executed in your PowerShell session, in order, along with what each one does.

Command	Purpose
cd myapp	Change into the myapp project directory.
php -r "file_put_contents('database/database.sqlite', '');"	Create an empty SQLite database file.
php artisan migrate	Run all pending database migrations (creates tables like users, cache, jobs).
php artisan migrate	Run migrations again. Laravel reports Nothing to migrate because they were already run.
php artisan config:clear	Clear the configuration cache.
php artisan route:clear	Clear the route cache.
php artisan serve	Start the Laravel development server at http://127.0.0.1:8000.
php artisan migrate	Check for pending migrations again.
php artisan serve	Start the server again.
php artisan tinker	Open Laravel's interactive PHP shell.
php artisan tinker	Open Tinker again after exiting.
php artisan make:seeder UserSeeder	Create a new seeder class named UserSeeder.
php artisan db:seed --class=UserSeeder	Attempt to run the UserSeeder. It failed because the class did not exist or contained invalid code.
php artisan serve	Start the development server.
php artisan migrate	Check migrations again.
php artisan serve	Start the server again.
php artisan tinker	Open Tinker to inspect users.
php artisan serve	Start the server again.
Commands executed inside Tinker
First Tinker session
$u = App\Models\User::where('email', 'your@email.com')->first();


Looks for a user with the email your@email.com.

Result:

null


No such user exists.

$u->role = 'admin';


❌ Error because $u is null.

$u->save();


❌ Error because $u is null.

exit


Exit Tinker.

Second Tinker session
$user = new App\Models\User();


Create a new User model.

$user->name = "Admin";

$user->email = "admin@gmail.com";

$user->password = bcrypt("12345678");


Hash the password.

$user->save();


Save the user to the database.

exit

Third Tinker session
App\Models\User::all();


Display all users in the database.

Output showed:

ID: 1
Name: Admin
Email: admin@gmail.com

Then:

save


❌ Error:

Undefined constant "save"


because save by itself is not a PHP command. It must be called on a model object, for example:

$user->save();

Errors encountered
1. User not found
$u = App\Models\User::where(...)->first();

= null


Cause:

There was no user with the email your@email.com.
2. Cannot assign property on null
Attempt to assign property "role" on null.


Cause:

$u was null.
3. Seeder error
Target class [Database\Seeders\UserSeeder] does not exist.


This usually happens because one of these occurred:

UserSeeder.php contains invalid PHP syntax.
The namespace is incorrect.
The class name doesn't match the filename.
The file accidentally contains pasted output instead of valid PHP code (your transcript suggests this happened, since the contents of the run() method appeared in the terminal before the error).
Summary

These are the main Laravel Artisan commands used:

cd myapp
php -r "file_put_contents('database/database.sqlite', '');"
php artisan migrate
php artisan config:clear
php artisan route:clear
php artisan serve
php artisan tinker
php artisan make:seeder UserSeeder
php artisan db:seed --class=UserSeeder


The only command that failed was:

php artisan db:seed --class=UserSeeder


because the UserSeeder class was not valid or could not be loaded. The rest of the commands completed successfully, and you successfully created an Admin user manually using Tinker.